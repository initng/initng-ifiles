FILE(GLOB II_FILES "*.ii")

PROCESS_IIFILES(I_FILES ${II_FILES})

INSTALL_FILES(${SYSCONF_INSTALL_DIR}/initng/debug FILES ${I_FILES})
INSTALL_PROGRAMS(${SYSCONF_INSTALL_DIR}/initng/debug FILES pidfile-daemon.sh) 
