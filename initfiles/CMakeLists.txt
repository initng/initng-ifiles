SUBDIRS(daemon net system service runlevel config)

IF(INSTALL_DEBUG)
	ADD_SUBDIRECTORY(debug)
ENDIF(INSTALL_DEBUG)

FILE(GLOB II_FILES "*.ii")

PROCESS_IIFILES(I_FILES ${II_FILES})

INSTALL_FILES(${SYSCONF_INSTALL_DIR}/initng FILES ${I_FILES})
